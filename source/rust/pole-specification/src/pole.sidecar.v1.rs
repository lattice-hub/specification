// This file is @generated by prost-build.
#[derive(Clone, PartialEq, Eq, Hash, ::prost::Message)]
pub struct ClientHello {
    #[prost(string, tag = "1")]
    pub sdk_language: ::prost::alloc::string::String,
    #[prost(string, tag = "2")]
    pub sdk_version: ::prost::alloc::string::String,
    #[prost(enumeration = "Protocol", repeated, tag = "3")]
    pub supported_protocols: ::prost::alloc::vec::Vec<i32>,
}
#[derive(Clone, PartialEq, Eq, Hash, ::prost::Message)]
pub struct ClientEvent {
    #[prost(oneof = "client_event::Event", tags = "1, 2, 3")]
    pub event: ::core::option::Option<client_event::Event>,
}
/// Nested message and enum types in `ClientEvent`.
pub mod client_event {
    #[derive(Clone, PartialEq, Eq, Hash, ::prost::Oneof)]
    pub enum Event {
        #[prost(message, tag = "1")]
        Hello(super::ClientHello),
        #[prost(message, tag = "2")]
        RegisterLocalService(super::LocalServiceRegistration),
        #[prost(message, tag = "3")]
        UnregisterLocalService(super::LocalServiceUnregistration),
    }
}
#[derive(Clone, Copy, PartialEq, Eq, Hash, ::prost::Message)]
pub struct Listener {
    #[prost(enumeration = "Protocol", tag = "1")]
    pub protocol: i32,
    #[prost(uint32, tag = "2")]
    pub port: u32,
}
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct ListenerSnapshot {
    /// These listeners are loopback-only outbound listeners used by the local
    /// Thin SDK. Ingress listeners are never exposed through bootstrap.
    #[prost(message, repeated, tag = "1")]
    pub listeners: ::prost::alloc::vec::Vec<Listener>,
}
#[derive(Clone, PartialEq, Eq, Hash, ::prost::Message)]
pub struct LocalServiceRegistration {
    /// Stable and unique within one OpenControlSession stream.
    #[prost(string, tag = "1")]
    pub registration_id: ::prost::alloc::string::String,
    #[prost(string, tag = "2")]
    pub namespace: ::prost::alloc::string::String,
    #[prost(string, tag = "3")]
    pub service: ::prost::alloc::string::String,
    #[prost(enumeration = "Protocol", tag = "4")]
    pub protocol: i32,
    /// The Sidecar always connects to this port through 127.0.0.1.
    #[prost(uint32, tag = "5")]
    pub local_port: u32,
}
#[derive(Clone, PartialEq, Eq, Hash, ::prost::Message)]
pub struct LocalServiceUnregistration {
    #[prost(string, tag = "1")]
    pub registration_id: ::prost::alloc::string::String,
}
#[derive(Clone, PartialEq, Eq, Hash, ::prost::Message)]
pub struct LocalServiceStatus {
    #[prost(string, tag = "1")]
    pub registration_id: ::prost::alloc::string::String,
    #[prost(enumeration = "LocalServiceState", tag = "2")]
    pub state: i32,
    #[prost(string, tag = "3")]
    pub message: ::prost::alloc::string::String,
}
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct SidecarEvent {
    #[prost(oneof = "sidecar_event::Event", tags = "1, 2")]
    pub event: ::core::option::Option<sidecar_event::Event>,
}
/// Nested message and enum types in `SidecarEvent`.
pub mod sidecar_event {
    #[derive(Clone, PartialEq, ::prost::Oneof)]
    pub enum Event {
        #[prost(message, tag = "1")]
        ListenerSnapshot(super::ListenerSnapshot),
        #[prost(message, tag = "2")]
        LocalServiceStatus(super::LocalServiceStatus),
    }
}
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash, PartialOrd, Ord, ::prost::Enumeration)]
#[repr(i32)]
pub enum Protocol {
    Unspecified = 0,
    Http = 1,
    Grpc = 2,
    Dubbo = 3,
    Thrift = 4,
}
impl Protocol {
    /// String value of the enum field names used in the ProtoBuf definition.
    ///
    /// The values are not transformed in any way and thus are considered stable
    /// (if the ProtoBuf definition does not change) and safe for programmatic use.
    pub fn as_str_name(&self) -> &'static str {
        match self {
            Self::Unspecified => "PROTOCOL_UNSPECIFIED",
            Self::Http => "PROTOCOL_HTTP",
            Self::Grpc => "PROTOCOL_GRPC",
            Self::Dubbo => "PROTOCOL_DUBBO",
            Self::Thrift => "PROTOCOL_THRIFT",
        }
    }
    /// Creates an enum from field names used in the ProtoBuf definition.
    pub fn from_str_name(value: &str) -> ::core::option::Option<Self> {
        match value {
            "PROTOCOL_UNSPECIFIED" => Some(Self::Unspecified),
            "PROTOCOL_HTTP" => Some(Self::Http),
            "PROTOCOL_GRPC" => Some(Self::Grpc),
            "PROTOCOL_DUBBO" => Some(Self::Dubbo),
            "PROTOCOL_THRIFT" => Some(Self::Thrift),
            _ => None,
        }
    }
}
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash, PartialOrd, Ord, ::prost::Enumeration)]
#[repr(i32)]
pub enum LocalServiceState {
    Unspecified = 0,
    Registered = 1,
    Unregistered = 2,
    Rejected = 3,
}
impl LocalServiceState {
    /// String value of the enum field names used in the ProtoBuf definition.
    ///
    /// The values are not transformed in any way and thus are considered stable
    /// (if the ProtoBuf definition does not change) and safe for programmatic use.
    pub fn as_str_name(&self) -> &'static str {
        match self {
            Self::Unspecified => "LOCAL_SERVICE_STATE_UNSPECIFIED",
            Self::Registered => "LOCAL_SERVICE_STATE_REGISTERED",
            Self::Unregistered => "LOCAL_SERVICE_STATE_UNREGISTERED",
            Self::Rejected => "LOCAL_SERVICE_STATE_REJECTED",
        }
    }
    /// Creates an enum from field names used in the ProtoBuf definition.
    pub fn from_str_name(value: &str) -> ::core::option::Option<Self> {
        match value {
            "LOCAL_SERVICE_STATE_UNSPECIFIED" => Some(Self::Unspecified),
            "LOCAL_SERVICE_STATE_REGISTERED" => Some(Self::Registered),
            "LOCAL_SERVICE_STATE_UNREGISTERED" => Some(Self::Unregistered),
            "LOCAL_SERVICE_STATE_REJECTED" => Some(Self::Rejected),
            _ => None,
        }
    }
}
/// Generated client implementations.
pub mod sidecar_session_service_client {
    #![allow(
        unused_variables,
        dead_code,
        missing_docs,
        clippy::wildcard_imports,
        clippy::let_unit_value,
    )]
    use tonic::codegen::*;
    use tonic::codegen::http::Uri;
    #[derive(Debug, Clone)]
    pub struct SidecarSessionServiceClient<T> {
        inner: tonic::client::Grpc<T>,
    }
    impl SidecarSessionServiceClient<tonic::transport::Channel> {
        /// Attempt to create a new client by connecting to a given endpoint.
        pub async fn connect<D>(dst: D) -> Result<Self, tonic::transport::Error>
        where
            D: TryInto<tonic::transport::Endpoint>,
            D::Error: Into<StdError>,
        {
            let conn = tonic::transport::Endpoint::new(dst)?.connect().await?;
            Ok(Self::new(conn))
        }
    }
    impl<T> SidecarSessionServiceClient<T>
    where
        T: tonic::client::GrpcService<tonic::body::Body>,
        T::Error: Into<StdError>,
        T::ResponseBody: Body<Data = Bytes> + std::marker::Send + 'static,
        <T::ResponseBody as Body>::Error: Into<StdError> + std::marker::Send,
    {
        pub fn new(inner: T) -> Self {
            let inner = tonic::client::Grpc::new(inner);
            Self { inner }
        }
        pub fn with_origin(inner: T, origin: Uri) -> Self {
            let inner = tonic::client::Grpc::with_origin(inner, origin);
            Self { inner }
        }
        pub fn with_interceptor<F>(
            inner: T,
            interceptor: F,
        ) -> SidecarSessionServiceClient<InterceptedService<T, F>>
        where
            F: tonic::service::Interceptor,
            T::ResponseBody: Default,
            T: tonic::codegen::Service<
                http::Request<tonic::body::Body>,
                Response = http::Response<
                    <T as tonic::client::GrpcService<tonic::body::Body>>::ResponseBody,
                >,
            >,
            <T as tonic::codegen::Service<
                http::Request<tonic::body::Body>,
            >>::Error: Into<StdError> + std::marker::Send + std::marker::Sync,
        {
            SidecarSessionServiceClient::new(InterceptedService::new(inner, interceptor))
        }
        /// Compress requests with the given encoding.
        ///
        /// This requires the server to support it otherwise it might respond with an
        /// error.
        #[must_use]
        pub fn send_compressed(mut self, encoding: CompressionEncoding) -> Self {
            self.inner = self.inner.send_compressed(encoding);
            self
        }
        /// Enable decompressing responses.
        #[must_use]
        pub fn accept_compressed(mut self, encoding: CompressionEncoding) -> Self {
            self.inner = self.inner.accept_compressed(encoding);
            self
        }
        /// Limits the maximum size of a decoded message.
        ///
        /// Default: `4MB`
        #[must_use]
        pub fn max_decoding_message_size(mut self, limit: usize) -> Self {
            self.inner = self.inner.max_decoding_message_size(limit);
            self
        }
        /// Limits the maximum size of an encoded message.
        ///
        /// Default: `usize::MAX`
        #[must_use]
        pub fn max_encoding_message_size(mut self, limit: usize) -> Self {
            self.inner = self.inner.max_encoding_message_size(limit);
            self
        }
        /// Legacy listener bootstrap retained for existing Thin SDKs.
        pub async fn open_session(
            &mut self,
            request: impl tonic::IntoRequest<super::ClientHello>,
        ) -> std::result::Result<
            tonic::Response<tonic::codec::Streaming<super::SidecarEvent>>,
            tonic::Status,
        > {
            self.inner
                .ready()
                .await
                .map_err(|e| {
                    tonic::Status::unknown(
                        format!("Service was not ready: {}", e.into()),
                    )
                })?;
            let codec = tonic_prost::ProstCodec::default();
            let path = http::uri::PathAndQuery::from_static(
                "/pole.sidecar.v1.SidecarSessionService/OpenSession",
            );
            let mut req = request.into_request();
            req.extensions_mut()
                .insert(
                    GrpcMethod::new(
                        "pole.sidecar.v1.SidecarSessionService",
                        "OpenSession",
                    ),
                );
            self.inner.server_streaming(req, path, codec).await
        }
        /// Long-lived Thin SDK control session. The first ClientEvent must contain
        /// ClientHello. Registrations belong to this stream and are removed when it
        /// closes.
        pub async fn open_control_session(
            &mut self,
            request: impl tonic::IntoStreamingRequest<Message = super::ClientEvent>,
        ) -> std::result::Result<
            tonic::Response<tonic::codec::Streaming<super::SidecarEvent>>,
            tonic::Status,
        > {
            self.inner
                .ready()
                .await
                .map_err(|e| {
                    tonic::Status::unknown(
                        format!("Service was not ready: {}", e.into()),
                    )
                })?;
            let codec = tonic_prost::ProstCodec::default();
            let path = http::uri::PathAndQuery::from_static(
                "/pole.sidecar.v1.SidecarSessionService/OpenControlSession",
            );
            let mut req = request.into_streaming_request();
            req.extensions_mut()
                .insert(
                    GrpcMethod::new(
                        "pole.sidecar.v1.SidecarSessionService",
                        "OpenControlSession",
                    ),
                );
            self.inner.streaming(req, path, codec).await
        }
    }
}
/// Generated server implementations.
pub mod sidecar_session_service_server {
    #![allow(
        unused_variables,
        dead_code,
        missing_docs,
        clippy::wildcard_imports,
        clippy::let_unit_value,
    )]
    use tonic::codegen::*;
    /// Generated trait containing gRPC methods that should be implemented for use with SidecarSessionServiceServer.
    #[async_trait]
    pub trait SidecarSessionService: std::marker::Send + std::marker::Sync + 'static {
        /// Server streaming response type for the OpenSession method.
        type OpenSessionStream: tonic::codegen::tokio_stream::Stream<
                Item = std::result::Result<super::SidecarEvent, tonic::Status>,
            >
            + std::marker::Send
            + 'static;
        /// Legacy listener bootstrap retained for existing Thin SDKs.
        async fn open_session(
            &self,
            request: tonic::Request<super::ClientHello>,
        ) -> std::result::Result<
            tonic::Response<Self::OpenSessionStream>,
            tonic::Status,
        >;
        /// Server streaming response type for the OpenControlSession method.
        type OpenControlSessionStream: tonic::codegen::tokio_stream::Stream<
                Item = std::result::Result<super::SidecarEvent, tonic::Status>,
            >
            + std::marker::Send
            + 'static;
        /// Long-lived Thin SDK control session. The first ClientEvent must contain
        /// ClientHello. Registrations belong to this stream and are removed when it
        /// closes.
        async fn open_control_session(
            &self,
            request: tonic::Request<tonic::Streaming<super::ClientEvent>>,
        ) -> std::result::Result<
            tonic::Response<Self::OpenControlSessionStream>,
            tonic::Status,
        >;
    }
    #[derive(Debug)]
    pub struct SidecarSessionServiceServer<T> {
        inner: Arc<T>,
        accept_compression_encodings: EnabledCompressionEncodings,
        send_compression_encodings: EnabledCompressionEncodings,
        max_decoding_message_size: Option<usize>,
        max_encoding_message_size: Option<usize>,
    }
    impl<T> SidecarSessionServiceServer<T> {
        pub fn new(inner: T) -> Self {
            Self::from_arc(Arc::new(inner))
        }
        pub fn from_arc(inner: Arc<T>) -> Self {
            Self {
                inner,
                accept_compression_encodings: Default::default(),
                send_compression_encodings: Default::default(),
                max_decoding_message_size: None,
                max_encoding_message_size: None,
            }
        }
        pub fn with_interceptor<F>(
            inner: T,
            interceptor: F,
        ) -> InterceptedService<Self, F>
        where
            F: tonic::service::Interceptor,
        {
            InterceptedService::new(Self::new(inner), interceptor)
        }
        /// Enable decompressing requests with the given encoding.
        #[must_use]
        pub fn accept_compressed(mut self, encoding: CompressionEncoding) -> Self {
            self.accept_compression_encodings.enable(encoding);
            self
        }
        /// Compress responses with the given encoding, if the client supports it.
        #[must_use]
        pub fn send_compressed(mut self, encoding: CompressionEncoding) -> Self {
            self.send_compression_encodings.enable(encoding);
            self
        }
        /// Limits the maximum size of a decoded message.
        ///
        /// Default: `4MB`
        #[must_use]
        pub fn max_decoding_message_size(mut self, limit: usize) -> Self {
            self.max_decoding_message_size = Some(limit);
            self
        }
        /// Limits the maximum size of an encoded message.
        ///
        /// Default: `usize::MAX`
        #[must_use]
        pub fn max_encoding_message_size(mut self, limit: usize) -> Self {
            self.max_encoding_message_size = Some(limit);
            self
        }
    }
    impl<T, B> tonic::codegen::Service<http::Request<B>>
    for SidecarSessionServiceServer<T>
    where
        T: SidecarSessionService,
        B: Body + std::marker::Send + 'static,
        B::Error: Into<StdError> + std::marker::Send + 'static,
    {
        type Response = http::Response<tonic::body::Body>;
        type Error = std::convert::Infallible;
        type Future = BoxFuture<Self::Response, Self::Error>;
        fn poll_ready(
            &mut self,
            _cx: &mut Context<'_>,
        ) -> Poll<std::result::Result<(), Self::Error>> {
            Poll::Ready(Ok(()))
        }
        fn call(&mut self, req: http::Request<B>) -> Self::Future {
            match req.uri().path() {
                "/pole.sidecar.v1.SidecarSessionService/OpenSession" => {
                    #[allow(non_camel_case_types)]
                    struct OpenSessionSvc<T: SidecarSessionService>(pub Arc<T>);
                    impl<
                        T: SidecarSessionService,
                    > tonic::server::ServerStreamingService<super::ClientHello>
                    for OpenSessionSvc<T> {
                        type Response = super::SidecarEvent;
                        type ResponseStream = T::OpenSessionStream;
                        type Future = BoxFuture<
                            tonic::Response<Self::ResponseStream>,
                            tonic::Status,
                        >;
                        fn call(
                            &mut self,
                            request: tonic::Request<super::ClientHello>,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move {
                                <T as SidecarSessionService>::open_session(&inner, request)
                                    .await
                            };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = OpenSessionSvc(inner);
                        let codec = tonic_prost::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.server_streaming(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                "/pole.sidecar.v1.SidecarSessionService/OpenControlSession" => {
                    #[allow(non_camel_case_types)]
                    struct OpenControlSessionSvc<T: SidecarSessionService>(pub Arc<T>);
                    impl<
                        T: SidecarSessionService,
                    > tonic::server::StreamingService<super::ClientEvent>
                    for OpenControlSessionSvc<T> {
                        type Response = super::SidecarEvent;
                        type ResponseStream = T::OpenControlSessionStream;
                        type Future = BoxFuture<
                            tonic::Response<Self::ResponseStream>,
                            tonic::Status,
                        >;
                        fn call(
                            &mut self,
                            request: tonic::Request<tonic::Streaming<super::ClientEvent>>,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move {
                                <T as SidecarSessionService>::open_control_session(
                                        &inner,
                                        request,
                                    )
                                    .await
                            };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = OpenControlSessionSvc(inner);
                        let codec = tonic_prost::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.streaming(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                _ => {
                    Box::pin(async move {
                        let mut response = http::Response::new(
                            tonic::body::Body::default(),
                        );
                        let headers = response.headers_mut();
                        headers
                            .insert(
                                tonic::Status::GRPC_STATUS,
                                (tonic::Code::Unimplemented as i32).into(),
                            );
                        headers
                            .insert(
                                http::header::CONTENT_TYPE,
                                tonic::metadata::GRPC_CONTENT_TYPE,
                            );
                        Ok(response)
                    })
                }
            }
        }
    }
    impl<T> Clone for SidecarSessionServiceServer<T> {
        fn clone(&self) -> Self {
            let inner = self.inner.clone();
            Self {
                inner,
                accept_compression_encodings: self.accept_compression_encodings,
                send_compression_encodings: self.send_compression_encodings,
                max_decoding_message_size: self.max_decoding_message_size,
                max_encoding_message_size: self.max_encoding_message_size,
            }
        }
    }
    /// Generated gRPC service name
    pub const SERVICE_NAME: &str = "pole.sidecar.v1.SidecarSessionService";
    impl<T> tonic::server::NamedService for SidecarSessionServiceServer<T> {
        const NAME: &'static str = SERVICE_NAME;
    }
}
